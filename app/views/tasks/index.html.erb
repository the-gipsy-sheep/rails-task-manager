<h1>Aca tenemos que listar todas las tareas</h1>

<% @tasks.each do |task| %>
  <h3><%= link_to "#{task.title}", task_path(task) %> - <%= task.completed ? "Done" : "To do" %></h3>
<% end %>

<%= link_to "Create a task", new_task_path %>
